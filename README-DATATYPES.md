# Data Types:
numbers
strings
Booleans
Arrays-(String arrays, number array, object array)
Objects:
 Behaviour Subject(we need to assign/update data in BS we have to use .next() method)

nested objects
selectors